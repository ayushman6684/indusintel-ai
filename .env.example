# This is a root-level pointer — see backend/.env.example and
# frontend/.env.local.example for the actual variables each service needs.
#
# Quick start:
#   cp backend/.env.example backend/.env
#   cp frontend/.env.local.example frontend/.env.local
